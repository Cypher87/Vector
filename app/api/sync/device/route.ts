import { disconnectDeviceResponse, renameDeviceResponse } from '../../../../src/server/sync-http';

export const dynamic = 'force-dynamic';
export const DELETE = disconnectDeviceResponse;
export const PATCH = renameDeviceResponse;
